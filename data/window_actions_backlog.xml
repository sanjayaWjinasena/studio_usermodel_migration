<?xml version='1.0' encoding='UTF-8'?>
<!-- studio_usermodel_migration backlog: 3 window actions --><odoo>

  <record id="act_window_1443_customer_groups" model="ir.actions.act_window">
    <field name="name">Customer Groups</field>
    <field name="res_model">x_customer_group</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

  <!-- STRIPPED act_window_2677_x_customer_groups: model x_customer_groups not on target --><record id="act_window_1444_vendor_groups" model="ir.actions.act_window">
    <field name="name">Vendor Groups</field>
    <field name="res_model">x_vendor_group</field>
    <field name="view_mode">tree,form</field>
    <field name="context">{}</field>
    <field name="help" type="html"><p class="o_view_nocontent_smiling_face">
                    This is your new action.
                </p>
                <p>By default, it contains a list and a form view and possibly
                    other view types depending on the options you chose for your model.
                </p>
                <p>
                    You can start customizing these screens by clicking on the Studio icon on the
                    top right corner (you can also customize this help message there).
                </p>
            </field>
  </record>

</odoo>